{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "cross-env PORT=4000 bun --hot src/index.ts",
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@types/shared": "workspace:*",
    "@utils/shared": "workspace:*",
    "hono": "^4.10.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "cross-env": "^10.1.0"
  }
}